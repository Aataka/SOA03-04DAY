# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
*.tfplan

# Lambda build artifact
lambda/*.zip

# 作業用一時スクリプト・ログ
_*.sh
_*.log

# 記事(docs)・学習足場(study)はGitHubに保管しない（ローカル限定）
docs/
study/
